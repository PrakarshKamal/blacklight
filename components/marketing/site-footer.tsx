export function SiteFooter() {
  return (
    <footer className="border-t border-zinc-800/80">
      <div className="mx-auto flex w-full max-w-7xl flex-col items-center justify-between gap-2 px-4 py-8 text-sm text-zinc-500 sm:flex-row sm:px-6">
        <p>Blacklight · AI firewall for file uploads</p>
        <p className="text-zinc-400">
        Antivirus secured computers. Blacklight secures what your AI reads.
        </p>
      </div>
    </footer>
  );
}
